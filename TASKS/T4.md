# T4 — Data Pipeline and Persistence

## Goal

Make the tray, egg result, API, CSV, database, and dashboard data consistent.

## Tasks

- [ ] Align `egg_statistics.csv` with the per-egg contract.
- [ ] Rename `class` to `damage_status`.
- [ ] Add `cleanliness_status`, `tray_id`, and `captured_at`.
- [ ] Add a CSV logger to the inference service.
- [ ] Add a `trays` table with capture time, image reference, and operator.
- [ ] Ensure the egg table stores detection, damage, cleanliness, geometry,
  weight, size, grade, and confidence fields.
- [ ] Add migrations for schema changes.
- [ ] Expose the intended API operations:
  - `POST /api/grade`
  - `GET /api/records`
  - `GET /api/stats`
- [ ] Preserve existing authenticated routes or document the migration.
- [ ] Return tray metadata plus one result per detected egg.
- [ ] Add database and API validation for allowed statuses and grades.
- [ ] Store model versions, thresholds, and calibration version per tray.

## Verification

- [ ] Submit an image containing 0–5 eggs.
- [ ] Verify one database/CSV record per detected egg.
- [ ] Verify API, CSV, and database field names match exactly.
- [ ] Verify failed inference does not create a completed-looking result.
